import React from 'react';
import { EnhancerIcon, ImageIcon, KLogo, MoonIcon, RealTimeIcon, SunIcon, VideoIcon } from '@/component/icons';

export default function page() {
  return (
    <div>
      <KLogo className='size-10' />
      <EnhancerIcon className='size-10' />
      <ImageIcon className='size-10' />
      <VideoIcon className='size-10' />
      <RealTimeIcon className='size-10' />
      <SunIcon className='size-10' />
      <MoonIcon className='size-10' />
    </div>
  )
}
